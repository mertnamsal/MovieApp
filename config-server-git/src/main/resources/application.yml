server:
  port: 8889

spring:
  application:
    name: config-server-git
  cloud:
    config:
      server:
        git:
          uri: https://github.com/mustafaozturk0614/ConfigServer.git //düzelmesi lazm
          default-label: main
          skip-ssl-validation: true
          username: mertnamsal
          password: ${GIT_PASSWORD}